pluginManagement {
    repositories {
        google()
        jcenter()
//        mavenCentral()
        gradlePluginPortal()


       maven { url 'https://jitpack.io' } // Add the JitPack repository
        jcenter() {
            content {
                includeModule("com.theartofdev.edmodo", "android-image-cropper")
            }
        }
    }
    dependencyResolutionManagement {
        repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
        repositories {
            google()
           jcenter()
//           mavenCentral()
        }
    }
    rootProject.name = "celebrare"
    include ':app'
}